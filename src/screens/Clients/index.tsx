import React from 'react';
import {Typographic} from '../../components/Typographic';

import {BaseScreen} from '../BaseScreen';

export const Clients: React.FC = () => {
  return (
    <BaseScreen>
      <Typographic.Title>Clients</Typographic.Title>
    </BaseScreen>
  );
};
